package session

import "testing"

//sstr := session.Session.GetString(r.Context(), skey)
// - setup a new db
// - setup a server pointing to that db
// - run some session stuff, (how to mock persistent request/cookie-header?)
// - delete test db
func TestSession(t *testing.T) {
	if true == false {
		t.Errorf("TestSession true == false")
	}
}
